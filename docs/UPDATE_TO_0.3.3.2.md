# Update to v0.3.3.2

This patch fixes stale GitHub Pages/PWA caching that could make v0.3.3 appear unchanged after a successful deployment.

Copy this release over the local repository, commit, and push. After GitHub Actions succeeds, reload the site. One extra reload may be needed for an already-installed service worker to activate.
