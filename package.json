{
  "name": "battle-axe-design-studio",
  "version": "0.3.3",
  "private": true,
  "description": "Integrated browser-based workspace for historical wargame map compilation, scenario design, deployment, playtesting, analytics, and publication.",
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "verify": "npm run check && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
