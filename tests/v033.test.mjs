import test from 'node:test'; import assert from 'node:assert/strict'; import {readFile} from 'node:fs/promises';
test('geometry detector includes raster-assisted hydrology and geometry-space coordinates',async()=>{const s=await readFile(new URL('../src/modules/battlefieldDetector.js',import.meta.url),'utf8');assert.match(s,/rasterHydrology/);assert.match(s,/getCTM/);assert.match(s,/derived review geometry only/);assert.match(s,/meaningfulInside/);});
test('map review uses sticky battlefield panel',async()=>{const s=await readFile(new URL('../src/styles/app.css',import.meta.url),'utf8');assert.match(s,/\.map-panel\{position:sticky/);});
test('feature and geometry review expose bulk-selection controls',async()=>{const html=await readFile(new URL('../index.html',import.meta.url),'utf8');assert.match(html,/bulkApproveFeatures/);assert.match(html,/bulkImportCandidates/);assert.match(html,/selectSimilarCandidates/);});
